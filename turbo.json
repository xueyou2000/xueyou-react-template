{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "cache": true
    },
    "build:all": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "cache": true
    },
    "preview": {},
    "analyze": {},
    "dev:ssr": {},
    "build:ssr": {},
    "lint": {},
    "stub": {},
    "dev": {
      "cache": true
    }
  }
}
